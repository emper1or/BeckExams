
HTTP:
- Версии протокола HTTP. Отличия
- Протокол HTTPS. Протокол TLS

## 2.1 Что такое HTTP

**HTTP (HyperText Transfer Protocol)** — протокол прикладного уровня для передачи данных между клиентом и сервером по модели _request → response_.

Работает поверх TCP (или QUIC в HTTP/3).

---

## 2.2 Версии HTTP и отличия

### HTTP/1.0

- один запрос = одно TCP-соединение
    
- медленный
    
- почти не используется
    

---

### HTTP/1.1

- keep-alive (одно соединение — много запросов)
    
- текстовый протокол
    
- проблемы:
    
    - head-of-line blocking
        
    - много лишних заголовков
        

---

### HTTP/2

- бинарный протокол
    
- мультиплексирование (несколько запросов параллельно)
    
- сжатие заголовков (HPACK)
    
- быстрее HTTP/1.1
    

---

### HTTP/3

- работает поверх **QUIC (UDP)**
    
- быстрее установление соединения
    
- устойчив к потере пакетов
    
- активно используется в браузерах
## 2.3 HTTPS

**HTTPS = HTTP + TLS**

Что даёт HTTPS:

- 🔐 шифрование данных
    
- 🧾 проверка подлинности сервера
    
- 🛡 защита от MITM-атак
    

---

## 2.4 TLS (Transport Layer Security)

TLS — криптографический протокол, который обеспечивает:

- шифрование
    
- целостность
    
- аутентификацию
    

### Как работает TLS (упрощённо)

1. Клиент запрашивает сертификат
    
2. Сервер отправляет публичный ключ
    
3. Клиент проверяет доверенный CA
    
4. Устанавливается защищённый канал