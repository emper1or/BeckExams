## 19) Реализация API для работы с пользователями
Реализация API манипуляции над пользователями:
- какой будет полный запрос c фильтром(поиском), сортировкой(asc,desc), выбором определенных полей, пагинацией
(придумать поля самостоятельно)
example.com/users....
- что обычно должен возвращать сервер (при запросе списков (коллекции), т.е получение всех данных Users), что должно быть в json